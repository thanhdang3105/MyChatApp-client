const roomsSelector = ({ rooms }) => rooms;
const currentRoomSelector = ({ currentRoom }) => currentRoom;
const usersSelector = ({ users }) => users;

export { roomsSelector, currentRoomSelector, usersSelector };
